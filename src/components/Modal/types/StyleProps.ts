export default interface StyleProps {
  fullscreen?: boolean;
  borderRadius?: string;
  background?: string;
  center?: boolean;
  height?: string;
  width?: string;
  shadow?: string;

  hideCloseButton?: boolean;
  onClose?: any;
  show?: boolean;
}
